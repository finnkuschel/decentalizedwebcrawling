With this iExec application you can fetch website data onto your smart contract in case a website doesn't provide an api.

ROADMAP
pass website url and specific css selectors as argument to look for and return 
